问题：在ceph 虚拟机里挂载限定数据池大小的cephfs,cephfs会限制最大文件输入，umount不会使内核崩溃。
	在Linux物理机里采取相同的操作，写入大文件马上umount会使内核崩溃。稍等一会再umount就不会出问题。
答： 虚拟机内存小，同步到数据缓存进程满足不了，所以会限制写入大文件的大小，而liunux物理机内存大，所以写入缓存大文件不会限制。
     可以在写入大文件后执行sync命令强行将缓存写入磁盘后再umount掉，这样内核就不会崩溃了。